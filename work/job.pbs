#!/bin/bash

#PBS -N mainline_nucleosynthesis
#PBS -l select=1:ncpus=32:mem=64gb:ngpus=1:gpu_model=v100
#PBS -l walltime=16:00:00
#PBS -j oe

cd $PBS_O_WORKDIR
if [ -z "${out_dir}" ]
then
    ./run_main.sh --model ${model}
else
    ./run_main.sh --model ${model} --out_dir ${out_dir}
fi
